# تشغيل التطبيق لأول مرة

## 1. إنشاء مشروع Firebase (مرة واحدة، حوالي ربع ساعة)
1. افتح https://console.firebase.google.com وسجّل دخول بحساب Google.
2. اختر Add project → اكتب اسم للمشروع (مثلاً alaelah-mart) → كمّل من غير Google Analytics.
3. من القائمة الجانبية: Build → Firestore Database → Create database → اختر Start in production mode → اختر المنطقة eur3.
4. من إعدادات المشروع (علامة الترس) → Your apps → دوس على أيقونة `</>` (Web) → سجّل التطبيق → انسخ كائن `firebaseConfig` اللي هيظهر.
5. الصق القيم في ملف `firebase-config.js` مكان `{}` في السطر بتاع `window.FIREBASE_CONFIG`.
6. ارجع لصفحة Firestore → تبويب Rules → امسح الموجود والصق محتوى ملف `firestore.rules` → دوس Publish.

## 2. النشر على GitHub Pages
1. ارفع المشروع على GitHub في repo عام.
2. من صفحة الريبو: Settings → Pages → Source: Deploy from a branch → Branch: main والمجلد root → Save.
3. بعد دقايق الرابط هيشتغل: `https://<اسم-المستخدم>.github.io/<اسم-الريبو>/`

## 3. تجربة على الموبايل (قائمة الفحص)
- افتح الرابط في Chrome على أندرويد.
- من قائمة المتصفح اختر "إضافة إلى الشاشة الرئيسية" → أيقونة خضراء هتظهر زي أي تطبيق.
- جرّب: شحنة جديدة → امسح باركود منتج حقيقي → الكاميرا تفتح وتقرأ → هتسمع صوت بيب.
- ارفض إذن الكاميرا مرة → هتظهر رسالة، وكتابة الباركود باليد هتشتغل عادي.
- فعّل وضع الطيران → اعمل شحنة → رجّع النت → افتح صفحة المدير (manager.html) → الشحنة هتكون موجودة.
- صفحة المدير ليها رابط منفصل: افتح `<رابط التطبيق>/manager.html` — الرقم السري في ملف firebase-config.js، غيّره قبل النشر.

## الفروع والأرقام السرية
| | الرقم | بيفتح إيه |
|---|---|---|
| فرع قويسنا | `1010` | الموظف يختار الفرع + مدير الفرع يشوف شحنات قويسنا بس |
| فرع شبين الكوم | `2020` | الموظف يختار الفرع + مدير الفرع يشوف شحنات شبين بس |
| المدير العام | `1994` | كل الفروع مع إمكانية الفلترة |

- أول ما الموظف يفتح التطبيق: يكتب اسمه، يختار الفرع، ويكتب رقم الفرع — بكده مفيش تسجيل على فرع غلط. الاختيار محفوظ على موبايله.
- يغيّر الفرع أو الاسم من الزرار اللي فيه اسمه فوق في الشاشة الرئيسية (هيطلب رقم الفرع تاني).
- صفحة المدير: لو دخلت برقم فرع، الصفحة تتقفل على الفرع ده والتحميل بيطلع بياناته بس.
- تغيير الأرقام أو إضافة فرع تالت: من `branches` جوه ملف `firebase-config.js`.

## استيراد أسماء الأصناف من Excel (اختياري، يوفّر كتابة الأسماء)
1. من صفحة المدير دوس «تحميل النموذج» → هيوصلك ملف فيه عمودين جاهزين.
2. املا الملف في Excel: الباركود في العمود الأول واسم الصنف في التاني.
3. حفظ باسم → اختر النوع **CSV UTF-8**.
4. صفحة المدير → «استيراد ملف» → اختر الملف.
5. بعدها أي موظف يمسح باركود موجود في الملف، الاسم هيتكتب لوحده.

## تحميل الشحنات (Excel أو TXT)
- شحنة واحدة: من قائمة الشحنات دوس **Excel** أو **TXT** جنب الشحنة (نفس الزرارين جوه الشحنة).
- كل الشحنات: تحت في «تصدير الشحنات» → **Excel** أو **TXT** — بيحمّل اللي ظاهر حسب فلتر الفرع.
- ملف Excel فيه أعمدة (الفرع، الشحنة، الموظف، التاريخ، الباركود، الاسم، الكمية) وبيفتح بالعربي على طول.
- ملف TXT فيه سطر لكل صنف: الباركود، ثم مسافة تاب، ثم الكمية — جاهز لأنظمة الكاشير.

## أسماء الأصناف
الموظف مش بيكتب اسم الصنف — الاسم بيظهر لوحده من ملف الأصناف المستورد. لو الباركود
مش موجود في الملف بتظهر جملة «صنف غير مسجّل في ملف الأصناف» وبرضه ينفع يضيفه بالكمية،
والباركود هو اللي يبان في الشحنة. عشان الأسماء تظهر، استورد الملف من صفحة المدير.

## ملاحظة أمان
الرابط نفسه هو الحماية الأساسية — متشاركوش مع حد غريب. البيانات المخزنة كلها
أسماء أصناف وكميات بس، مفيش أي بيانات حساسة.
ولو الريبو عام على GitHub، أي حد ممكن يعرف الرابط — عشان كده غيّر الرقم السري وخلّي اسم الريبو مش سهل التخمين.
