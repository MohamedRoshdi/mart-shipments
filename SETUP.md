# تشغيل التطبيق لأول مرة

## 1. إنشاء مشروع Firebase (مرة واحدة، حوالي ربع ساعة)
1. افتح https://console.firebase.google.com وسجّل دخول بحساب Google.
2. اختر Add project → اكتب اسم للمشروع (مثلاً alaelah-mart) → كمّل من غير Google Analytics.
3. من القائمة الجانبية: Build → Firestore Database → Create database → اختر Start in production mode → اختر المنطقة eur3.
4. من إعدادات المشروع (علامة الترس) → Your apps → دوس على أيقونة `</>` (Web) → سجّل التطبيق → انسخ كائن `firebaseConfig` اللي هيظهر.
5. الصق القيم في ملف `firebase-config.js` مكان `{}` في السطر بتاع `window.FIREBASE_CONFIG`.
6. ارجع لصفحة Firestore → تبويب Rules → امسح الموجود والصق محتوى ملف `firestore.rules` → دوس Publish.

## 2. النشر على GitHub Pages
1. ارفع المشروع على GitHub في repo عام.
2. من صفحة الريبو: Settings → Pages → Source: Deploy from a branch → Branch: main والمجلد root → Save.
3. بعد دقايق الرابط هيشتغل: `https://<اسم-المستخدم>.github.io/<اسم-الريبو>/`

## 3. تجربة على الموبايل (قائمة الفحص)
- افتح الرابط في Chrome على أندرويد.
- من قائمة المتصفح اختر "إضافة إلى الشاشة الرئيسية" → أيقونة خضراء هتظهر زي أي تطبيق.
- جرّب: شحنة جديدة → امسح باركود منتج حقيقي → الكاميرا تفتح وتقرأ → هتسمع صوت بيب.
- ارفض إذن الكاميرا مرة → هتظهر رسالة، وكتابة الباركود باليد هتشتغل عادي.
- فعّل وضع الطيران → اعمل شحنة → رجّع النت → افتح شاشة المدير → الشحنة هتكون موجودة.
- الرقم السري لشاشة المدير: 2580 (تقدر تغيّره في ملف firebase-config.js).

## ملاحظة أمان
الرابط نفسه هو الحماية الأساسية — متشاركوش مع حد غريب. البيانات المخزنة كلها
أسماء أصناف وكميات بس، مفيش أي بيانات حساسة.
