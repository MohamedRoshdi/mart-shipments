# تشغيل التطبيق لأول مرة

## 1. إنشاء مشروع Firebase (مرة واحدة، حوالي ربع ساعة)
1. افتح https://console.firebase.google.com وسجّل دخول بحساب Google.
2. اختر Add project → اكتب اسم للمشروع (مثلاً alaelah-mart) → كمّل من غير Google Analytics.
3. من القائمة الجانبية: Build → Firestore Database → Create database → اختر Start in production mode → اختر المنطقة eur3.
4. من إعدادات المشروع (علامة الترس) → Your apps → دوس على أيقونة `</>` (Web) → سجّل التطبيق → انسخ كائن `firebaseConfig` اللي هيظهر.
5. الصق القيم في ملف `firebase-config.js` مكان `{}` في السطر بتاع `window.FIREBASE_CONFIG`.
6. ارجع لصفحة Firestore → تبويب Rules → امسح الموجود والصق محتوى ملف `firestore.rules` → دوس Publish.

## 2. النشر على GitHub Pages
1. ارفع المشروع على GitHub في repo عام.
2. من صفحة الريبو: Settings → Pages → Source: Deploy from a branch → Branch: main والمجلد root → Save.
3. بعد دقايق الرابط هيشتغل: `https://<اسم-المستخدم>.github.io/<اسم-الريبو>/`

## 3. تجربة على الموبايل (قائمة الفحص)
- افتح الرابط في Chrome على أندرويد.
- من قائمة المتصفح اختر "إضافة إلى الشاشة الرئيسية" → أيقونة خضراء هتظهر زي أي تطبيق.
- جرّب: شحنة جديدة → امسح باركود منتج حقيقي → الكاميرا تفتح وتقرأ → هتسمع صوت بيب.
- ارفض إذن الكاميرا مرة → هتظهر رسالة، وكتابة الباركود باليد هتشتغل عادي.
- فعّل وضع الطيران → اعمل شحنة → رجّع النت → افتح صفحة المدير (manager.html) → الشحنة هتكون موجودة.
- صفحة المدير ليها رابط منفصل: افتح `<رابط التطبيق>/manager.html` — الرقم السري في ملف firebase-config.js، غيّره قبل النشر.

## الفروع والأرقام السرية
| | الرقم | بيفتح إيه |
|---|---|---|
| فرع قويسنا | `1010` | الموظف يختار الفرع + مدير الفرع يشوف شحنات قويسنا بس |
| فرع شبين الكوم | `2020` | الموظف يختار الفرع + مدير الفرع يشوف شحنات شبين بس |
| المدير العام | `1994` | كل الفروع مع إمكانية الفلترة |
| الأدمن | `7007` | صفحة النظام: الإعدادات وآخر العمليات والأدوات الخطرة (وكمان بيفتح صفحة المدير على كل الفروع) |

- أول ما الموظف يفتح التطبيق: يكتب اسمه، يختار الفرع، ويكتب رقم الفرع — بكده مفيش تسجيل على فرع غلط. الاختيار محفوظ على موبايله.
- يغيّر الفرع أو الاسم من الزرار اللي فيه اسمه فوق في الشاشة الرئيسية (هيطلب رقم الفرع تاني).
- صفحة المدير: لو دخلت برقم فرع، الصفحة تتقفل على الفرع ده والتحميل بيطلع بياناته بس.
- تغيير الأرقام أو إضافة فرع تالت: من **صفحة النظام** (تحت)، من غير ما تلمس أي ملف.

## المستخدمين والصلاحيات (الطريقة الجديدة)
من صفحة النظام، أول قسم اسمه **المستخدمين**. لكل واحد بيشتغل عندك:

1. دوس **«+ إضافة مستخدم»**.
2. اكتب اسمه ورقم سري خاص بيه (من 4 لـ 8 أرقام، ومينفعش رقم يتكرر مع حد تاني).
3. اختار فرعه، أو «كل الفروع» للمدير العام.
4. دوس على الصلاحيات اللي تخصه — الزرار اللي عليه علامة ✓ يبقى مفتوح:

| الصلاحية | معناها |
|---|---|
| شاشة الموظف | يفتح شاشة المسح والشحنات بتاعته |
| شاشة المدير | يفتح قائمة الشحنات كلها في فرعه |
| شاشة النظام | يفتح الإعدادات دي نفسها |
| إنشاء شحنة | يعمل شحنة جديدة ويمسح فيها |
| تعديل الشحنات | يغيّر الكميات والأسماء ويشيل أصناف |
| حذف الشحنات | يحذف شحنة كاملة |
| تحميل الملفات | نسخ، Excel، TXT، وملف ZIP |
| إدارة الأصناف | يفتح شاشة الأصناف ويعدّل ويحذف |
| استيراد الأصناف | يرفع ملف أصناف جديد |
| الأدوات الخطرة | الحذف بالجملة ومسح ملف الأصناف |

5. دوس **«حفظ الإعدادات»**.

بعد كده الموظف يفتح **الرابط الرئيسي بس** (مش لازم يعرف روابط تانية)، يكتب رقمه،
والتطبيق يوديه على طول للشاشة بتاعته. اللي مالوش صلاحية فيه مش بيظهر له خالص —
مثلاً اللي مالوش «حذف الشحنات» مش هيلاقي زرار حذف أصلاً.

نقط مهمة:
- **الأرقام القديمة لسه شغالة** (رقم الفرع ورقم المدير ورقم الأدمن)، عشان لو حصلت أي
  مشكلة في المستخدمين تفضل قادر تدخل. لو عايز تقفلها خالص كلّمني.
- الاسم اللي بيتسجل على الشحنة وفي «آخر العمليات» هو اسم المستخدم اللي داخل.
- الجلسة بتفضل مفتوحة ١٢ ساعة على الموبايل، وبعدين بيطلب الرقم تاني. وفيه زرار **«خروج»**
  في كل شاشة لو حد عايز يسلّم الموبايل لواحد تاني.

## صفحة النظام (للأدمن بس)
الرابط: `<رابط التطبيق>/admin.html` — أو من صفحة المدير: «إعدادات النظام» → «فتح صفحة النظام».
الرقم السري `7007`. تنفع تتثبّت على الموبايل كتطبيق لوحدها زي صفحة المدير.

جواها أربع حاجات:

**1. الفروع** — تضيف فرع، تغيّر اسمه، تغيّر رقمه السري، أو تشيله. الشحنات القديمة
بتفضل زي ما هي حتى لو شيلت الفرع.

**2. أنواع الشحنات** — تضيف نوع جديد أو تشيل نوع. النوع الجديد بيظهر للموظفين
وفي فلاتر المدير على طول.

**3. الأرقام السرية** — رقم المدير العام ورقم الأدمن. الرقم من 4 لـ 8 أرقام.

بعد أي تعديل دوس **حفظ الإعدادات**. الموبايلات بتشوف التعديل مع أول فتح للتطبيق.
مهم: رقم الأدمن الأصلي (`7007`) بيفضل شغال دايمًا كصمام أمان — يعني لو حصل أي لغبطة
في الأرقام، لسه بتقدر تدخل بيه وتصلّح.

**4. آخر العمليات** — من زرار «آخر العمليات» فوق. سجل بيقول: مين عمل إيه وامتى
(حذف شحنة، تعديل شحنة، حذف صنف، تعديل أسماء، استيراد أصناف، تعديل الإعدادات).
السجل ده مش بينفع يتعدّل ولا يتحذف — لا منك ولا من غيرك.

**الأدوات الخطرة** (مفيش رجوع بعدها، والتطبيق يسألك تأكيد الأول):
- **حذف شحنات بالجملة**: اختار الفرع والنوع، والزرار بيوريك عدد الشحنات المطابقة قبل الحذف.
- **مسح ملف الأصناف**: بيشيل كل الأصناف. بعده مفيش باركود هيتقبل في أي شحنة لحد ما
  تستورد الملف تاني — خُد نسخة بزرار «تحميل ملف الأصناف» قبل ما تعمل كده.

## استيراد أسماء الأصناف من Excel (اختياري، يوفّر كتابة الأسماء)
1. من صفحة المدير دوس «تحميل النموذج» → هيوصلك ملف فيه عمودين جاهزين.
2. املا الملف في Excel: الباركود في العمود الأول واسم الصنف في التاني.
3. حفظ باسم → اختر النوع **CSV UTF-8**.
4. صفحة المدير → «استيراد ملف» → اختر الملف.
5. بعدها أي موظف يمسح باركود موجود في الملف، الاسم هيتكتب لوحده.

## نوع الشحنة
- في شاشة «شحنة جديدة» الموظف يشوف الفرع، وتحته يختار نوع الشحنة: **إذن استلام** (الافتراضي) أو **إذن مرتجع** أو **تحويل فرع**.
- النوع بيظهر مع كل شحنة في شاشة الموظف وشاشة المدير.
- في صفحة المدير فيه فلتر «نوع الشحنة» تحت فلتر الفرع، والمدير يقدر يصحّح النوع من جوه الشحنة.
- ملف Excel للشحنات فيه عمود «نوع الشحنة».
- تعديل الأنواع أو زيادة نوع: من **صفحة النظام** → «أنواع الشحنات».

## تحميل الشحنات (Excel أو TXT)
- شحنة واحدة: من قائمة الشحنات دوس **Excel** أو **TXT** جنب الشحنة (نفس الزرارين جوه الشحنة).
- كل الشحنات: تحت في «تصدير الشحنات» → **Excel** أو **TXT** — بيحمّل اللي ظاهر حسب فلتر الفرع.
- **ZIP بالمجلدات**: ملف مضغوط واحد، جواه مجلد باسم كل نوع شحنة، وجوه كل مجلد الشحنة
  في ملف Excel وملف TXT. الشكل بيبقى كده:

```
شحنات-2026-07-30.zip
├── إذن استلام/
│   ├── شحنة اللحمة.csv
│   └── شحنة اللحمة.txt
├── إذن مرتجع/
│   └── مرتجع الألبان.csv
└── تحويل فرع/
    └── تحويل 12.csv
```

  على الموبايل: افتح الملف من «التنزيلات» وهو هيفتح المجلدات عادي. على الكمبيوتر:
  كليك يمين → Extract. ولو اتنين شحنة بنفس الاسم في نفس النوع، التانية بتاخد `(2)`.
- ملف Excel فيه أعمدة (الفرع، الشحنة، الموظف، التاريخ، الباركود، الاسم، الكمية) وبيفتح بالعربي على طول.
- ملف TXT فيه سطر لكل صنف: الباركود، ثم مسافة تاب، ثم الكمية — جاهز لأنظمة الكاشير.

## شاشة الأصناف (من صفحة المدير → «عرض الأصناف»)
- بتوريك كل الأصناف المستوردة: الاسم فوق والباركود تحته.
- **البحث**: اكتب أول الاسم أو الباركود — البحث بيدور في كل الأصناف مهما كان عددها.
- **تعديل الاسم**: عدّل الاسم في مكانه ثم «حفظ تعديل الأسماء».
- **حذف صنف**: زرار × جنب الصنف (بيسأل قبل الحذف).
- **تحميل ملف الأصناف**: بينزّل كل الأصناف CSV — كمان بيصلح كنسخة احتياطية تعيد استيرادها.
- الشاشة بتحمّل أول ٣٠٠ صنف للعرض، والباقي توصله بالبحث.

## لو الكاميرا مش بتقرأ كويس على موبايل معيّن
في التطبيق زرار **«الكاميرا»** فوق في الشريط (على شاشة «شحناتي» وشاشة «شحنة جديدة»).
الإعدادات دي محفوظة على الموبايل نفسه — كل موبايل وإعداداته.

جرّب بالترتيب ده:

1. **غيّر الكاميرا.** ده أهم حاجة. كتير من الموبايلات فيها أكتر من كاميرا خلفية،
   وفي واحدة منهم (الواسعة) مش بتعرف تركّز على حاجة قريبة، فالباركود يطلع مش واضح.
   في «الكاميرا» هتلاقي قائمة بكاميرات الموبايل — جرّب واحدة واحدة على باركود صغير،
   واللي تقرا أسرع سيبها.
2. **قرّب الباركود شوية بالتقريب** لو الباركود صغير. لو الصورة بقت مشوّشة رجّعه لـ 1.
3. **افتح الفلاش** لو الرف ضلمة. ينفع تفتحه وتقفله وإنت ماسح من زرار «الفلاش»
   اللي بيظهر تحت الكاميرا.
4. **كبّر منطقة المسح** لو بتصوّر من بعيد شوية، أو صغّرها لو فيه باركودات كتير جنب بعض
   وبيقرا الغلط.
5. امسح والباركود مستوي مش مايل، والموبايل ثابت لحظة، والمسافة حوالي 10–15 سم.

ملاحظة: التطبيق بيستخدم قارئ الباركود المدمج في أندرويد لو موجود — ده أسرع وأدق من
القراءة بالجافاسكريبت، وبيشتغل لوحده من غير أي إعداد.

## أسماء الأصناف — والأصناف المرفوضة
- الموظف مش بيكتب اسم الصنف؛ الاسم بيظهر لوحده من ملف الأصناف المستورد.
- **أي باركود مش موجود في ملف الأصناف مش هينفع يتسجّل في الشحنة.** بتظهر للموظف رسالة
  توضّح إن الباركود مش موجود وإن الصنف مش هيتسجّل، وإن المفروض يبعت الباركود للمدير
  ليضيفه في ملف الأصناف، وفيه زرار «نسخ الباركود» يسهّل عليه إرساله. زرار «إضافة الصنف»
  بيكون مقفول.
- المدير يضيف الصنف الناقص من صفحة المدير: «استيراد ملف» بملف فيه الباركود والاسم.

## ملاحظة أمان
الرابط نفسه هو الحماية الأساسية — متشاركوش مع حد غريب. البيانات المخزنة كلها
أسماء أصناف وكميات بس، مفيش أي بيانات حساسة.
ولو الريبو عام على GitHub، أي حد ممكن يعرف الرابط — عشان كده غيّر الرقم السري وخلّي اسم الريبو مش سهل التخمين.

كلام صريح عن حدود الحماية: الأرقام السرية دي أبواب في الشاشة، مش حماية حقيقية. اللي
عنده الرابط يقدر — لو عرف يتعامل مع النظام — يقرا الشحنات والأصناف ويعدّلها، وكمان يغيّر
الإعدادات المحفوظة. عشان كده رقم الأدمن اللي في الكود بيفضل شغال دايمًا (باب خلفي ليك
إنت بس)، وسجل «آخر العمليات» مقفول ضد التعديل والحذف. الحماية الحقيقية الوحيدة هي إن
الرابط ما يوصلش لحد بره المحل. لو حسّيت إن الرابط اتسرّب، كلّمني نضيف تسجيل دخول حقيقي.
